# frozen_string_literal: true

RSpec.describe Kitchen::Yansible::Pusher do
  it "has a version number" do
    expect(Kitchen::Yansible::Pusher::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
